# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
.venv/
venv/
build/
dist/

# uv
.python-version

# Nuitka build artifacts
*.build/
*.dist/
*.onefile-build/

# Windows/NSIS
*.exe
*.msi

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Reports / logs
*.log
gpu_report*.json

# Per-machine OpenCL correctness-test cache (robo_rec.gpu.correctness_cache) — this
# is deliberately meant to travel with a copied app folder (e.g. on a flash drive),
# never with the source checkout.
gpu_config.json

# Test VM state (if ever referenced locally)
*.vdi
*.vbox
